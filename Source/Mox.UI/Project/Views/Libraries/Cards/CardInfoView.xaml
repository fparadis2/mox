<UserControl x:Class="Mox.UI.Library.CardInfoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ui="clr-namespace:Mox.UI"
             xmlns:library="clr-namespace:Mox.UI.Library"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance {x:Type library:CardViewModel_DesignTime}, IsDesignTimeCreatable=True}"
             Width="300">
    <Border Padding="4">
        <StackPanel>
            <ui:CardFrame Card="{Binding CurrentPrinting.Card}" />
            
            <Grid Margin="0,8,0,0">
                <Grid.Resources>
                    <Style x:Key="KeyTextBlockStyle" TargetType="TextBlock">
                        <Setter Property="TextAlignment" Value="Right" />
                        <Setter Property="Margin" Value="0,0,4,0"></Setter>
                    </Style>
                    <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
                </Grid.Resources>
                
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Card:" Style="{StaticResource KeyTextBlockStyle}" />
                <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding Name}" />

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Mana Cost:" Style="{StaticResource KeyTextBlockStyle}" Visibility="{Binding HasManaCost, Converter={StaticResource BooleanToVisibilityConverter}}" />
                <ui:SymbolTextBlock Grid.Row="1" Grid.Column="1" Text="{Binding ManaCostText}" Visibility="{Binding HasManaCost, Converter={StaticResource BooleanToVisibilityConverter}}" />

                <TextBlock Grid.Row="2" Grid.Column="0" Text="Types:" Style="{StaticResource KeyTextBlockStyle}" />
                <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding TypeLine}" />

                <TextBlock Grid.Row="3" Grid.Column="0" Text="Card Text:" Style="{StaticResource KeyTextBlockStyle}" />
                <ui:SymbolTextBlock Grid.Row="3" Grid.Column="1" Text="{Binding Rules}" />

                <TextBlock Grid.Row="4" Grid.Column="0" Text="Flavor Text:" Style="{StaticResource KeyTextBlockStyle}" Visibility="{Binding CurrentPrinting.HasFlavorText, Converter={StaticResource BooleanToVisibilityConverter}}" />
                <TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding CurrentPrinting.FlavorText}" FontStyle="Italic" TextWrapping="Wrap" Visibility="{Binding CurrentPrinting.HasFlavorText, Converter={StaticResource BooleanToVisibilityConverter}}" />

                <TextBlock Grid.Row="5" Grid.Column="0" Text="P/T:" Style="{StaticResource KeyTextBlockStyle}" Visibility="{Binding HasPowerToughness, Converter={StaticResource BooleanToVisibilityConverter}}" />
                <TextBlock Grid.Row="5" Grid.Column="1" Text="{Binding PowerToughnessText}" Visibility="{Binding HasPowerToughness, Converter={StaticResource BooleanToVisibilityConverter}}" />

                <TextBlock Grid.Row="6" Grid.Column="0" Text="Set:" Style="{StaticResource KeyTextBlockStyle}" />
                <TextBlock Grid.Row="6" Grid.Column="1" Text="{Binding CurrentPrinting.SetName}" />

                <TextBlock Grid.Row="7" Grid.Column="0" Text="Rarity:" Style="{StaticResource KeyTextBlockStyle}" />
                <TextBlock Grid.Row="7" Grid.Column="1" Text="{Binding CurrentPrinting.RarityText}" />

                <TextBlock Grid.Row="8" Grid.Column="0" Text="Artist:" Style="{StaticResource KeyTextBlockStyle}" />
                <TextBlock Grid.Row="8" Grid.Column="1" Text="{Binding CurrentPrinting.ArtistName}" />
            </Grid>
            
        </StackPanel>
    </Border>
</UserControl>