<UserControl x:Class="Mox.UI.Library.CardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ui="clr-namespace:Mox.UI"
             xmlns:library="clr-namespace:Mox.UI.Library"
             xmlns:cal="http://www.caliburnproject.org"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance {x:Type library:CardViewModel_DesignTime}, IsDesignTimeCreatable=True}"
             cal:Bind.AtDesignTime="True"
             MinHeight="160" d:DesignWidth="800">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="116" />
            <ColumnDefinition Width="*" MinWidth="350" />
            <ColumnDefinition Width="160" />
        </Grid.ColumnDefinitions>

        <Image Grid.Column="0" ui:ImageService.Key="{Binding CurrentPrinting.FullCardImage}" Margin="8" VerticalAlignment="Top" />

        <StackPanel Grid.Column="1" Margin="8">
            <ui:SymbolTextBlock Text="{Binding HeaderText}" />
            <Rectangle Height="4" />
            <ui:SymbolTextBlock Text="{Binding Rules}" ItalicizeParenthesis="True" />
        </StackPanel>
        
        <StackPanel Grid.Column="2" Margin="8">
            <Image ui:ImageService.Key="{Binding CurrentPrinting.SetImage}" ToolTip="{Binding CurrentPrinting.ToolTip}" VerticalAlignment="Top" HorizontalAlignment="Center" Height="20" />
            <Rectangle Fill="Blue" Height="1" Margin="16,4,16,4" Visibility="{Binding HasMoreThanOnePrinting, Converter={StaticResource BooleanToVisibilityConverter}}" />
            <library:CardOtherPrintingsView HorizontalContentAlignment="Center" />
        </StackPanel>
        
    </Grid>
</UserControl>
