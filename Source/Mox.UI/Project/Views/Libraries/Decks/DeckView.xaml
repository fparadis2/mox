<UserControl x:Class="Mox.UI.Library.DeckView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:library="clr-namespace:Mox.UI.Library"
             xmlns:cal="http://www.caliburnproject.org"
             xmlns:ui="clr-namespace:Mox.UI"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="379"
             d:DataContext="{d:DesignInstance {x:Type library:DeckViewModel_DesignTime}, IsDesignTimeCreatable=True}"
             cal:Bind.AtDesignTime="True">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <DockPanel Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" LastChildFill="True">
            <Button DockPanel.Dock="Right" Content="Edit" Margin="4" Width="50" />
            <TextBlock Text="{Binding Name}" FontSize="24" FontFamily="Segoe UI Semilight" TextTrimming="CharacterEllipsis" />
        </DockPanel>

        <TextBlock Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Text="{Binding Description}" />
        
        <library:DeckCardListingView Grid.Row="2" Grid.Column="0" />
        
        <StackPanel Grid.Row="2" Grid.Column="1">
            <StackPanel Margin="4" Orientation="Horizontal" HorizontalAlignment="Right">
                <TextBlock Text="Sort by" VerticalAlignment="Center" Margin="0,0,4,0" />
                <ComboBox SelectedValue="{Binding CardGrouping}" SelectedValuePath="Tag">
                    <ComboBoxItem Content="Overview" Tag="{x:Static library:DeckCardGrouping.Overview}" />
                </ComboBox>
            </StackPanel>
            <Image Margin="4" ui:ImageService.Key="{Binding HoveredCard.Image}" Width="160" />
        </StackPanel>
    </Grid>
</UserControl>
