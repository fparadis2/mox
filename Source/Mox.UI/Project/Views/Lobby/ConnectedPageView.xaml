<UserControl x:Class="Mox.UI.Lobby.ConnectedPageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:lobby="clr-namespace:Mox.UI.Lobby"
             xmlns:cal="http://www.caliburnproject.org"
             xmlns:fa="http://schemas.fontawesome.io/icons/"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             d:DataContext="{d:DesignInstance {x:Type lobby:ConnectedPageViewModel_DesignTime}, IsDesignTimeCreatable=True}"
             cal:Bind.AtDesignTime="True">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="4 2">
            <Button Command="{Binding CloseCommand}" Style="{DynamicResource ChromelessButtonStyle}">
                <fa:FontAwesome Icon="SignOut" ToolTip="Disconnect" VerticalAlignment="Center" FontSize="15" />
            </Button>
            
            <TextBlock Text="{Binding ServerName}" Margin="12 0" />
        </StackPanel>
        
        <ContentControl Grid.Row="1" cal:View.Model="{Binding ActiveItem}" />
    </Grid>
</UserControl>
