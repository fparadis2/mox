<UserControl x:Class="Mox.UI.Game.GameTablePartView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Mox.UI.Game"
             xmlns:UI="clr-namespace:Mox.UI"
             mc:Ignorable="d" 
             d:DesignHeight="462" d:DesignWidth="865"
             UI:DesignTime.DataContext="{x:Type local:GameTablePartViewModel_DesignTime}">
	
    <UserControl.Resources>
    	<BooleanToVisibilityConverter x:Key="booleanToVisibilityConverter" />
    </UserControl.Resources>
    
	<Grid>
		<Grid>
			<Grid.RowDefinitions>
				<RowDefinition Height="*" />
                <RowDefinition Height="2" />
                <RowDefinition Height="*" />
			</Grid.RowDefinitions>
            <Border Grid.Row="0" Background="#5C1D1D" CornerRadius="3">
            	<Grid>
            		<local:BattlefieldControl DataContext="{Binding Path=Game.OpponentPlayer.Battlefield}" />
                    <local:StepsControl DataContext="{Binding Game}" Visibility="{Binding Path=IsNotActivePlayer, Converter={StaticResource booleanToVisibilityConverter}}" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="4" />
            	</Grid>
            </Border>
            <Border Grid.Row="2" Background="#4E495B" CornerRadius="3">
            	<Grid>
            		<local:BattlefieldControl DataContext="{Binding Path=Game.MainPlayer.Battlefield}" />
                    <local:StepsControl DataContext="{Binding Game}" Visibility="{Binding Path=IsActivePlayer, Converter={StaticResource booleanToVisibilityConverter}}" VerticalAlignment="Top" HorizontalAlignment="Right" Margin="4" />
            	</Grid>
            </Border>
		</Grid>
        <local:UserChoiceControl DataContext="{Binding Game.Interaction}" HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="4" />
    </Grid>
</UserControl>
