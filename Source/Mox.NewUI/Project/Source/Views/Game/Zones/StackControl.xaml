<UserControl
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:Mox.UI.Game" 
    xmlns:UI="clr-namespace:Mox.UI"
    x:Class="Mox.UI.Game.StackControl"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d"
    UI:DesignTime.DataContext="{x:Type local:SpellStackViewModel_DesignTime}">
        
    <Grid>
        <Grid.Resources>
            <Style TargetType="{x:Type ListBox}" BasedOn="{StaticResource ChromelessListBoxStyle}">
                <Setter Property="ItemsPanel">
                    <Setter.Value>
                        <ItemsPanelTemplate>
                            <local:StackFanPanel HorizontalAlignment="Left" VerticalAlignment="Center" SelectedIndex="{Binding SelectedIndex, ElementName=_Stack}" />
                        </ItemsPanelTemplate>
                    </Setter.Value>
                </Setter>
                <Setter Property="ItemTemplate">
                    <Setter.Value>
                        <DataTemplate>
                            <local:SpellControl />
                        </DataTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </Grid.Resources>

        <ListBox ClipToBounds="True" x:Name="_Stack" ItemsSource="{Binding Spells}"
                 MouseMove="ListBox_MouseMove" MouseLeave="ListBox_MouseLeave" />
    </Grid>
</UserControl>
