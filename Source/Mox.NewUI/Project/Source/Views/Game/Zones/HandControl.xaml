<UserControl
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:Mox.UI.Game" 
    xmlns:UI="clr-namespace:Mox.UI" 
    x:Class="Mox.UI.Game.HandControl"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    mc:Ignorable="d"
    UI:DesignTime.DataContext="{x:Type local:CardCollectionViewModel_DesignTime}" d:DesignWidth="278.667" d:DesignHeight="35.333">
            
    <UserControl.Resources>
        
        <Style TargetType="{x:Type ListBoxItem}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Grid>                                
                            <ContentPresenter Content="{TemplateBinding Content}" ContentTemplate="{TemplateBinding ContentTemplate}" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" Margin="{TemplateBinding Padding}" />
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="{x:Type ListBox}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate>
                        <ItemsPresenter />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="ItemsPanel">
                <Setter.Value>
                    <ItemsPanelTemplate>
                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="ItemTemplate">
            	<Setter.Value>
            		<DataTemplate>
                        <Button Content="{Binding Name}" UI:ButtonAction.BindTo="Choose" />
                    </DataTemplate>
            	</Setter.Value>
            </Setter>
        </Style>

    </UserControl.Resources>
    
    <Border SnapsToDevicePixels="True" DockPanel.Dock="Top" Background="{DynamicResource ButtonDefaultBrush}" CornerRadius="3">
        <ListBox ItemsSource="{Binding}" Focusable="False" x:Name="hand" MouseMove="ListBox_MouseMove" MouseLeave="ListBox_MouseLeave" />
    </Border>
</UserControl>
