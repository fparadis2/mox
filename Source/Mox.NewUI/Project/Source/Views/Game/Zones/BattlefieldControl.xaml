<UserControl
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:Mox.UI.Game" 
    xmlns:UI="clr-namespace:Mox.UI"
    x:Class="Mox.UI.Game.BattlefieldControl"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d"
    UI:DesignTime.DataContext="{x:Type local:CardCollectionViewModel_DesignTime}">
        
    <Grid>
        <Grid.Resources>

        	<Style TargetType="{x:Type ItemsControl}">
        		<Setter Property="ItemsPanel">
        			<Setter.Value>
        				<ItemsPanelTemplate>
        					<StackPanel Orientation="Horizontal"
        						VerticalAlignment="Center"
        						HorizontalAlignment="Center"/>
        				</ItemsPanelTemplate>
        			</Setter.Value>
        		</Setter>
        		<Setter Property="ItemTemplate">
        			<Setter.Value>
        				<DataTemplate>
                            <Button Content="{Binding Name}" UI:ButtonAction.BindTo="Choose" />
                        </DataTemplate>
        			</Setter.Value>
        		</Setter>
        	</Style>

        </Grid.Resources>
        
    	<ItemsControl ItemsSource="{Binding}" Focusable="False" />
    </Grid>
</UserControl>
